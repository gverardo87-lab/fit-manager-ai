interface:
  display_name: "FitManager Responsive Adaptive UI"
  short_description: "Adapt CRM pages responsively without flattening their identity."
  default_prompt: "Use $fitmanager-responsive-adaptive-ui to optimize this FitManager screen for mobile and tablet while preserving the page's desktop identity, density, and hierarchy."
