interface:
  display_name: "FitManager Dashboard CRM Design"
  short_description: "Refine overview hierarchy without overbuilding the page."
  default_prompt: "Use $fitmanager-dashboard-crm-design to refine this FitManager overview surface with faster scan, cleaner hierarchy, and explicit next-click paths."
